trigger:
-  ⁠master

pool:
  name: Default

steps:
- script: echo azure
  displayName: 'Run a one-line script'

- script: gradle build
  displayName: 'build'

- script: gradle run
  displayName: 'run'